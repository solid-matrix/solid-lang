# Tests for the irgen subproject: test cases will be added along with
# the IR generation implementation.
# NOTE: once a test executable links LLVM-C, it needs LLVM-C.dll at
# runtime; add a POST_BUILD copy like cli/CMakeLists.txt does.
# No test targets yet.
